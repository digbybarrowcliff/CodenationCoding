function timeSinceBirth () {
    const startTime = date.now(1998, 05, 10);
    const result = testfn();
    const endTime = date.now(2022, 11, 16)

    console.log (`Time since your birthday is ${endTime - startTime}`)
    return result
}



// petDeath () {
//     if (this.health === 0)
// }
// if health === 0;
// $().click(function(){
//     $().trigger(death);
// })

// setTimeout (petDeath)